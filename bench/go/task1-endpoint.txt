Add a new middleware function `WithRetry(max int, backoff time.Duration) Middleware` to the endpoint package. It should wrap an Endpoint so that if the endpoint returns an error, it retries up to max times with the given backoff between attempts. Follow the existing middleware patterns in the package (look at how Chain and WithTimeout are structured). Place it in endpoint/endpoint.go. Do not modify any other files.
