Refactor the log package: rename the exported function `NewJSONLogger(w io.Writer) Logger` to `NewStructuredLogger(w io.Writer) Logger`. Its behavior must stay identical (the JSON output format stays unchanged). Update every caller across the repository so the whole module still compiles — including all test and example files that reference it. Do not change any other exported API. After renaming, verify the build of ./log/... .
