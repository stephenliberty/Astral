In the transport/http package, add a new function `NewLoggingErrorEncoder(logger log.Logger, next ErrorEncoder) ErrorEncoder` that returns an ErrorEncoder which logs the error (using the "err" key) before delegating to next. Follow the existing patterns in the transport/http package (look at how ErrorEncoder and DefaultErrorEncoder are structured). Place it in transport/http/logging_error_encoder.go. Do not modify any other files.
