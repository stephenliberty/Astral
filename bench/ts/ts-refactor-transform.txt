Refactor the `packages/openapi-typescript` package: rename the exported default function `transformMediaTypeObject` to `transformSchemaMediaType` in `packages/openapi-typescript/src/transform/media-type-object.ts`. Its behavior must stay identical. Update every importer across the package (there are several files importing the old name), so the package still typechecks. The function is invoked by reference as a transformer (e.g. in `transform/index.ts` and by sibling transforms), not only by direct call, so check each import. Do not change any other exported API. After renaming, verify `tsc --noEmit -p tsconfig.json` from `packages/openapi-typescript/` passes.
