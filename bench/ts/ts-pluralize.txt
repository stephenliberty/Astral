In `packages/openapi-typescript/src/lib/utils.ts`, add a small exported helper function `pluralize(quantity: number, singular: string, plural?: string): string` that returns `1 sheep` / `2 sheep` style output (using the singular form when quantity is 1, the plural otherwise, and defaulting the plural to `singular + "s"`). Follow the existing conventions in the file: export the function, add a short JSDoc comment above it, and keep the same formatting style as the neighboring `formatTime` helper. Place it at the end of the file. Do not modify any other files.
