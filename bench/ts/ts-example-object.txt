The `packages/openapi-typescript/src/transform/` directory contains one file per OpenAPI object transform: `header-object.ts`, `parameter-object.ts`, `request-body-object.ts`, etc. Each file exports a default function that converts the OpenAPI object into a TypeScript AST type using `ts.factory`.

The transform directory is missing a `example-object.ts`. Create `packages/openapi-typescript/src/transform/example-object.ts` exporting a default `transformExampleObject(exampleObject: ExampleObject, options: TransformNodeOptions): ts.TypeNode`. The `ExampleObject` and `TransformNodeOptions` types are already defined in `packages/openapi-typescript/src/types.ts` (ExampleObject has optional `summary`, `description`, `value`, `externalValue`). Emit a TypeScript type-literal node exposing the example value under a `value` property of type `any`, and an optional `externalValue` property of type `string` using `QUESTION_TOKEN` when the field is absent. Follow the exact conventions of the neighboring transforms: build `string` and `any` keyword type nodes from `../lib/ts.ts` (note the `STRING` and `ANY`/`UNKNOWN` exports), use `tsPropertyIndex`, and add a JSDoc comment block. Do not modify any other files.
