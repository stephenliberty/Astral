The `packages/openapi-typescript/src/transform/` directory contains one file per OpenAPI object transform: `header-object.ts`, `parameter-object.ts`, `request-body-object.ts`, etc. Each file exports a default function, e.g. `transformHeaderObject(headerObject, options): ts.TypeNode`, that converts the OpenAPI object into a TypeScript AST type using `ts.factory`.

The transform directory is missing a `link-object.ts`. Create `packages/openapi-typescript/src/transform/link-object.ts` exporting a default `transformLinkObject(linkObject: LinkObject, options: TransformNodeOptions): ts.TypeNode`. The `LinkObject` and `TransformNodeOptions` types are already defined in `packages/openapi-typescript/src/types.ts`. Emit a TypeScript type-literal node describing the link: optional `operationRef`, `operationId`, `description`, `requestBody`, and a `parameters` index signature mapping string parameter names to their values. Follow the exact conventions of the neighboring transforms (imports from `../lib/ts.ts` and `../types.ts`, `tsPropertyIndex`, `QUESTION_TOKEN`, JSDoc comment block). Do not modify any other files.
